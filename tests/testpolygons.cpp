#include <iostream>
#include "opencv2/core/core.hpp"
#include "opencv2/highgui/highgui.hpp"
//#include "../shape.hpp"
#include "../polygons.hpp"

using namespace cv;

int main(){
  // Testing constructor and print functions
  /*std::vector< cv::Point2f > boundingBox;
  boundingBox.push_back( cv::Point2f(0.0, 0.0) );
  //boundingBox.push_back( cv::Point2f(10.0, 0.0) );
  boundingBox.push_back( cv::Point2f(100.0, 100.0) );
  //boundingBox.push_back( cv::Point2f(0.0, 10.0) );
  Polygons< cv::Point2f > p( boundingBox, 4 );
  p.printVertices();*/
  return 0;
}
